{{ form_start(form, { attr: { class: "space-y-6" } }) }}
<div class="space-y-4">
    {{ form_widget(form, { attr: { class: "block w-full p-3 border border-gray-300 rounded-lg shadow-sm focus:ring-orange-500 focus:border-orange-500 sm:text-sm" } }) }}
</div>
<div class="text-right">
    <button class="bg-orange-500 text-white px-6 py-3 rounded-lg shadow hover:bg-orange-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-orange-500">
        {{ button_label|default('Save') }}
    </button>
</div>
{{ form_end(form) }}
