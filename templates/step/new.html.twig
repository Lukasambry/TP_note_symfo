{% extends 'base.html.twig' %}

{% block title %}New Step{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-2xl w-full bg-white p-8 rounded-lg shadow-lg">
            <h1 class="text-2xl font-bold text-gray-900 mb-6">Create New Step</h1>
            {{ include('step/_form.html.twig') }}
            <div class="mt-4 text-center">
                <a href="{{ path('app_step_index') }}" class="text-blue-500 hover:underline">Back to List</a>
            </div>
        </div>
    </div>
{% endblock %}
