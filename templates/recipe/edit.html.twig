{% extends 'base.html.twig' %}

{% block title %}Edit Recipe{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-2xl w-full bg-white p-8 rounded-lg shadow-lg">
            <h1 class="text-3xl font-bold text-gray-900 mb-6">Edit Recipe</h1>
            {{ include('recipe/_form.html.twig', {'button_label': 'Update'}) }}
            <div class="mt-4 flex justify-between">
                <a href="{{ path('app_recipe_index') }}" class="text-blue-500 hover:underline">Back to List</a>
                <div>
                    {{ include('recipe/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
